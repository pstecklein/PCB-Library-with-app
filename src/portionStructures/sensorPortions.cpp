#include "sensorPortions.h"


void sensorPortions::modifyPortions(int busId, int addr, int data, int op, int status) {
    busId = busId;
    addr = addr;
    data = data;
    op = op;
    status = status;
}